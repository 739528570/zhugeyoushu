<!--index.wxml-->
<view class="container">
	<van-nav-bar title="TEST" placeholder bind:click-left="handleUpload" />
	<van-search value="{{ value }}" placeholder="请输入搜索关键词" />
	<van-grid gutter="{{ 10 }}" wx:if="{{ !loading }}">
		<van-grid-item clickable use-slot>
			<upload bind:onOk="getList" />
		</van-grid-item>
		<van-grid-item
		 clickable
		 icon="photo-o"
		 text="{{item.title}}"
		 wx:for="{{ list }}"
		 wx:key="_id"
		/>
	</van-grid>
	<footer active="index" />
</view>

